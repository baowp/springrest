package feature;


public class AutoClose {
    /*
     * @Test public void test() { t(); System.gc(); System.gc(); } private void
     * t() { A a = new A(); try { a.close(); } catch (Exception e) {
     * e.printStackTrace(); throw new RuntimeException(e); } } private class A
     * implements AutoCloseable {
     * @Override public void close() throws Exception {
     * System.out.println("A.close"); } }
     */
}
